Pod::Tree::HTML
- accept any $dest that support a print() method

t/template.t
add tests for the new destinations

perl2html
- template support?

mod2html - program to generate a properly linked tree of module pods in HTML
